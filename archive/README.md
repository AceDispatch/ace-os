# archive/ — retired artifacts (Forgetting Clause)

Where superseded campaign outputs go once their data lives in Supabase. **Currently EMPTY** —
archiving of the existing per-region CSV campaigns is **PARKED by operator decision (2026-06-25, "keep live")**.
Nothing is ever deleted; archived items remain in git history and on disk.
